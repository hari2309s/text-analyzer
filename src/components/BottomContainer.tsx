import styled from '@emotion/styled/macro';
import React from 'react';

const BottomContainer = () => {
  return <Container>hallo!</Container>;
};

const Container = styled.div({
  background: '#d7dedd',
  width: '70vw',
  height: '100px',
  borderRadius: '3px',
});

export default BottomContainer;
